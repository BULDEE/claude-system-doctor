{
  "name": "claude-system-doctor",
  "version": "0.1.2",
  "private": true,
  "description": "Scheduled audit and safe auto-heal plugin for Claude Code installations",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
